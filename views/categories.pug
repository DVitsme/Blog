extends layout

block content
  .container
    h1 #{title}
    ul.list-group
      each category, i in categories
        li.list-group-item
          a(class="btn btn-block text-left" href='/articles/category/'+category._id) #{category.title}
