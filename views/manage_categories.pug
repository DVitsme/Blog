extends layout

block content
  .container
    h1 #{title}
    table.table.table-striped
      tr
        th Category Title 
        th 
      each category, i in categories
        tr
          td #{category.title}
          td
            a.btn.btn-info(href='/manage/categories/edit/'+category._id) Edit
